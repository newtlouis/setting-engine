# CTA Resources Folder

Place your resource files (PDFs, videos, images) in this folder.

They will be automatically uploaded when a commenter uses the matching CTA keyword.

## Current Keywords Mapping

Configure the mapping in `melanie.config.js` under `outreach.cta_resources`.
